spring:
  datasource:
    url: jdbc:h2:mem:testmock
    username: testdb
    password: psw1234
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    hibernate.ddl-auto: update
  h2:
    console.enabled: true
